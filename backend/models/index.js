const product_model = require('./product.model');
const employees_model = require('./employees.model');
const gestion_model =require('./gestion.model');


module.exports = Object.freeze({
    product_model,
    employees_model,
    gestion_model
})
