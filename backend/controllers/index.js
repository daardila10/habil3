const product_controller = require('./product.controller');
const employees_controller = require('./employees.controler');
const gestion_controler=require('./gestion.controler')

module.exports = Object.freeze({
    product_controller, 
    employees_controller,
    gestion_controler

})